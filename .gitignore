node_modules
.pnpm-store
dist
build
.next
.turbo
coverage
.cache
*.log
*.tsbuildinfo
.cache/

.env
.env.local
.env.*.local
.env.production
!.env.example
!.env.production.example
# Vendor secret bundles (Alipay / Shouqianba / etc.) — drop the whole folder
# in repo root and have git ignore it so we never accidentally commit private
# keys. The folder name pattern matches "密钥<timestamp>".
密钥*/
密钥*.txt

.DS_Store
Thumbs.db

.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

apps/web/dist
apps/api/dist
apps/worker-*/dist
packages/*/dist
packages/db/prisma/migrations/.shadow

docker/data/
*.local

.dev-logs/

# Local Shopyy webhook sim payloads (contain real PII / payment secrets)
scripts/shopyy-webhook-sim/payloads/
